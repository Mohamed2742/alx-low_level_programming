#include <stdio.h>

/**
 * main - this users program prints exactly the string
 * followed by a new line.
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\" programming is like building a multilingual puzzle");

	return (0);
}
