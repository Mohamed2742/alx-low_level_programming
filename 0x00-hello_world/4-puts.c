#include <stdio.h>

/**
 *main -Entry point
 * 
 * Return: Always 0  (Success)
 */
int main(void)
 {
	 puts( "\"programming is like building a multilingual puzzle ");
	 echo -e "Anything\n"
       
          return (0);
 }
