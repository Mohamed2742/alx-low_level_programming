#!/bin/bash
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
 {
	 char test_str[] = "\"programming is like building a multilingual puzzle";

	 puts(test_str);
	 return (0);
 }
